import "./description.styles.scss";
import React from "react";

const Description = ({ name, types, weight, imgUrl, stats, abilities, id }) => {
  return <div className="description"></div>;
};

export default Description;
